import PlatformLayout from '../../components/common/PlatformLayout'
import { entisenseData } from '../../data/platformsData'

export default function Entisense() {
  return <PlatformLayout data={entisenseData} />
}
